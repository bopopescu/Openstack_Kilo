#!/bin/bash
git --init
#git init --bare 
git config --global user.name "spring"
git config --global user.email "yuyezhulan@163.com"
#git init --bare --shared

#sh-keygen   # enter a few times to accept defaults
#ssh-copy-id -i ~/.ssh/id_rsa spring@172.16.0.26
#vi ~/.git/config
#[receive]
#        denyCurrentBranch = ignore

